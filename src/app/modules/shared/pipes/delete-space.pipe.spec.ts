import { DeleteSpacePipe } from './delete-space.pipe';

describe('DeleteSpacePipe', () => {
  it('create an instance', () => {
    const pipe = new DeleteSpacePipe();
    expect(pipe).toBeTruthy();
  });
});
